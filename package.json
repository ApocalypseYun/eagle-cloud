{
  "name": "eagle-cloud-sync",
  "version": "0.1.1",
  "description": "Multi-device cloud synchronization plugin for Eagle",
  "private": true,
  "engines": {
    "node": ">=16"
  },
  "scripts": {
    "build": "esbuild src/background/index.ts --bundle --platform=node --target=node16 --outdir=background && esbuild src/window/index.ts --bundle --platform=node --target=node16 --outdir=window && cp src/window/index.html src/window/styles.css window/",
    "dev": "esbuild src/background/index.ts --bundle --platform=node --target=node16 --outdir=background --watch",
    "package": "./scripts/package.sh",
    "clean": "rm -rf background window dist"
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "typescript": "^5.4.0",
    "@types/node": "^16.18.0"
  }
}
